# Setups
## keyd
- sudo stow -t / keyd

## Helium Browser
- install "helium-browser-bin" from the aur
- add `export BROWSER=helium-browser` to `.config/uwsm/default`

## Bitwarden
#### Enabling Fingerprints
- ...
#### SSH Setup
- Make sure to import the fish config
- Enable SSH Agent in Settings > SSH Agent
